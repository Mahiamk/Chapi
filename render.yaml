services:
  # PostgreSQL Database (Corrected)
  - type: pserv
    name: waitlist-db
    env: postgres
    plan: starter
    databaseName: waitlist    # 👈 Move and rename POSTGRES_DB
    user: waitlist_user       # 👈 Move and rename POSTGRES_USER
    postgres:
      version: "15"
    envVars:
      - key: POSTGRES_PASSWORD  # 👈 Only the password (or other custom vars) stays here
        generateValue: true

  # FastAPI Backend
  - type: web
    name: waitlist-backend
    env: docker
    plan: starter
    healthCheckPath: /health
    # 👇 ADD THIS LINE 👇
    rootDir: ./backend
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: DATABASE_URL
        fromService:
          type: pserv
          name: waitlist-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_PASSWORD
        value: admin123
      - key: RESEND_API_KEY
        sync: false
      - key: FRONTEND_URL
        value: https://waitlist-frontend.onrender.com

  # Next.js Frontend
  - type: web
    name: waitlist-frontend
    env: docker
    plan: starter
    # 👇 ADD THIS LINE 👇
    rootDir: ./frontend
    dockerfilePath: ./frontend/Dockerfile
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: waitlist-backend
          envVarKey: RENDER_EXTERNAL_URL